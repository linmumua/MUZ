# CLAUDE.md - doudizhu-paper 项目提示词

## 项目定位

- 你正在开发 `doudizhu-paper`，当前插件名为 `MUZ`，这是一个运行在 Paper / Purpur 上的实体牌桌卡牌插件。
- 本项目的核心领域是：
  - **斗地主实体牌桌交互与回合流程**
  - **德州牌桌扩展与通用牌桌渲染能力**
- 本项目不是通用商店、邮箱、经济面板或无关管理插件，后续开发、重构、命名、配置与文案都必须围绕“牌桌玩法、实体渲染、交互手感、回合状态”展开。
- 不要把无关业务概念混进来，不要复用与当前牌桌系统无关的结构设计。

## 核心目标

- 通过 `ItemDisplay / TextDisplay / Interaction` 在世界中构建稳定的实体牌桌、椅子、按钮、手牌、上一手和状态信息。
- 提供完整的斗地主流程：
  - 大厅
  - 准备
  - 叫分
  - 加倍
  - 出牌
  - 结算
- 支持机器人、超时托管、提示出牌、动作提示、音效提示、背景音乐切换。
- 提供玩家个人显示设置与管理员全局渲染调节能力。
- 通过 CraftEngine 资源包与 bundle 同步，稳定提供卡牌、美术、按钮、家具资源。
- 保证桌边渲染稳定，不闪烁、不乱转、不重复刷实体，不因为刷新策略导致显示回退。
- 支持牌桌持久化、恢复、玩家偏好存储、战局记录等基础存储能力。
- 保持斗地主与德州模块在“显示风格、交互手感、配置方式”上的一致性，但不要强行混淆两者的业务逻辑。

## 沟通与编码规则

- 使用中文编写说明、讨论、设计记录与必要注释。
- 禁止使用 Unicode 转义表示中文或 `§` 颜色符号，必须直接书写原始字符。
- 禁止删除已有注释；若注释有误，只允许修正，不要直接删掉。
- 代码标识符、包名、类名、方法名、配置键、资源键统一使用英文。
- 玩家可见文本优先复用现有的 `Adventure Component`、`MiniMessage`、`MuzTheme`、`TypewriterTextStyle` 体系，不要到处散落风格不一致的字符串。
- 禁止把 Bukkit / Paper 主线程敏感逻辑放到异步线程中执行：
  - 实体生成
  - 实体 teleport / hide / show
  - 背包操作
  - 玩家传送
  - 世界交互
- 数据存储、网络请求、AI 请求等耗时操作应避免阻塞主线程。
- 禁止在代码中滥用全限定类名，统一通过 `import` 引入后使用短名。
- 禁止使用 Java 原生反射去硬搞 Bukkit / Paper / 第三方插件；如确实需要兼容层，请优先通过已有 `compat` 结构下沉隔离。
- 新增功能时必须同时考虑：
  - 配置默认值
  - GUI 展示
  - 文案一致性
  - 渲染刷新链路
  - 玩家旧数据兼容
- 涉及显示实体刷新时，优先复用现有实体并做增量更新，避免全量 remove/spawn 导致闪烁。

## 玩家消息与文本规范

所有面向玩家的文本消息必须优先通过现有 Adventure 组件体系发送，保持视觉风格一致。

### 当前推荐方式

```java
player.sendActionBar(MuzTheme.warning("请先选择要出的牌"));
player.sendMessage(MuzTheme.success("牌桌已经创建完成"));
player.sendMessage(MiniMessage.miniMessage().deserialize("<gold>你打出了</gold> <yellow>顺子</yellow>"));
```

### 推荐约束

- 优先使用：
  - `MuzTheme`
  - `TypewriterTextStyle`
  - `MiniMessage`
  - `Component`
- 禁止直接硬编码裸 `§` 彩色长字符串作为主要输出方式。
- 管理员调试日志可以直接使用中文。
- 玩家消息应区分用途：
  - `sendActionBar`：短反馈、即时提示、回合提示
  - `sendMessage`：明确通知、错误原因、阶段变化
  - `TextDisplay`：桌边常驻信息、状态展示、视觉标签
- 新增显示文本时，要先判断它属于：
  - 玩家聊天消息
  - 动作栏反馈
  - 桌边悬浮文字
  - GUI lore / title
  - 音效/动作触发文案
  不要混用。

## 构建

```bash
./gradlew build
```

构建流程当前包含但不限于：

- `compileJava`
- `jar`
- `reobfJar`
- `zipCraftEngineBundle`
- `zipResourcePack`

说明：

- `build` 会同时处理插件 jar 与资源包 / CraftEngine bundle 相关产物。
- 资源包相关内容来自：
  - `resourcepack/`
  - `build.gradle.kts` 中的生成任务
- 修改卡牌材质、按钮资源、声音资源时，必须考虑构建链路是否会重新生成 bundle。

## 当前技术栈

| 层 | 技术 | 版本 |
|---|---|---|
| 平台 | Paper / Purpur API | 1.21.11 |
| 构建 | paperweight userdev | 2.0.0-beta.19 |
| 语言 | Java | 当前主实现为 Java |
| 文本 | Adventure Component / MiniMessage | - |
| 资源 | CraftEngine bundle + 自定义 resourcepack | namespace=`muz` |
| 占位符 | PlaceholderAPI | 2.11.7 |
| 经济 | Vault / EzEconomy / XConomy | - |
| 兼容 | CMI / CMILib / CraftEngine | - |
| AI | 自定义 AI/第三方网关接入 | 位于 `ai/` |
| 插件名 | MUZ | 1.7.1 |

## 领域模型约束

本项目至少要保持以下概念分层，不要重新揉成一个 God Object：

- `GameTable`：斗地主核心牌局状态机，负责阶段推进与回合规则。
- `PhysicalTableManager`：斗地主实体牌桌渲染与交互管理。
- `ZjhTable`：德州/另一牌桌模式的核心状态。
- `ZjhPhysicalTableManager`：德州牌桌实体渲染与交互管理。
- `HandGuiService`：玩家个人设置、动作设置、管理员显示调节 GUI。
- `TableEffectCoordinator`：音效、语音提示、剩余手牌提醒等效果编排。
- `TableMusicCoordinator`：背景音乐与阶段音乐切换。
- `BotAiCoordinator`：机器人出牌/叫分/决策调度。
- `PackSounds`：声音事件定义与资源键映射。
- `PackAssets`：卡牌、按钮、家具模型资源映射。
- `compat/*`：第三方插件兼容层，避免业务逻辑直接散落到主流程。
- `storage/*`：持久化、恢复、记录相关逻辑。
- `room/*`：房间级别、倍率、桌级参数等规则抽象。
- `model/*`：牌型、牌面、分析器、基础规则模型。

## 功能边界

### 1. 斗地主核心对局

- 大厅入座 / 离座 / 准备 / 开始
- 叫分 / 抢分
- 加倍 / 不加倍
- 出牌 / 不要
- 牌型比较
- 炸弹 / 王炸倍率
- 结算与分数变更

### 2. 世界实体渲染

- 桌子 / 椅子 / 按钮 / 手牌 / 公共出牌区 / 状态条 / 顶部头像名
- 座位头像、名字、附属信息分离显示
- 顶栏头像、名字、状态正文分离显示
- 玩家自己的私有手牌与公共可见实体区分渲染
- 预览牌、上一手、桌边信息必须保持稳定刷新

### 3. 玩家交互

- Interaction 点击牌 / 按钮 / 座位
- 左键出牌 / 右键选择 等既定交互约定
- 提示出牌、清空选择、阶段按钮
- GUI 设置与管理员调参
- hover / selected 动画与颜色反馈

### 4. 音效与动作表现

- 出牌语音
- 倒计时音效
- 剩余 3 张 / 2 张提醒
- 回合音效、结算音效
- 可按牌型配置动作方案与提示方式

### 5. 存储与恢复

- 牌桌持久化
- 启动恢复
- 玩家视觉设置
- 历史记录 / 对局记录
- 兼容 SQLite / 现有存储链路

### 6. 资源包与 CraftEngine

- 卡牌模型
- UI 物件
- 家具模型
- 音效资源
- CraftEngine bundle 生成与同步

## 存储与资源设计原则

### 存储

- 当前项目已有 `storage/` 结构，后续存储逻辑应继续下沉，不要把 SQL / 文件读写塞进 `GameTable`。
- 玩家偏好、牌桌恢复、对局历史等应集中管理。
- 新增持久化字段时，必须考虑老数据兼容与默认值兜底。

### 资源

- 资源包源目录为 `resourcepack/`
- 构建输出会生成：
  - 资源包产物
  - CraftEngine bundle
- 命名空间当前为：
  - 源资源：`doudizhupaper`
  - 输出 bundle：`muz`
- 修改模型、贴图、音效时，必须确认：
  - 构建任务是否会重新生成
  - 旧路径是否还被引用
  - `PackAssets` / `PackSounds` 是否同步更新

### 一致性

- 渲染状态以 `GameTable / ZjhTable` 的当前状态为准。
- 世界实体只负责表现，不得成为唯一状态源。
- 按钮、预览牌、顶部状态条必须以增量更新为优先策略。
- 不要让“显示方便”反向污染规则层。

## 推荐包结构

```text
dev.mumu.doudizhu
├── action/                # 动作、表现、触发定义
├── ai/                    # 机器人与第三方 AI 网关
├── assets/                # PackAssets / PackSounds 等资源映射
├── command/               # /muz 等命令
├── compat/                # CraftEngine / Vault / PlaceholderAPI 等兼容层
├── game/                  # 斗地主核心状态机与协调器
├── listener/              # Bukkit 事件监听
├── model/                 # 牌、牌型、分析器、规则基础模型
├── placeholder/           # PAPI 占位符
├── room/                  # 房间级别、倍率、桌级参数
├── storage/               # 持久化、记录、恢复
├── ui/                    # HandGui / 主题 / 文本样式
├── world/                 # 斗地主实体牌桌渲染与世界交互
└── zhajinhua/             # 德州/另一牌桌模式相关实现
```

## 资源文件建议

```text
src/main/resources/
├── config.yml
├── gui-icons.yml
└── plugin.yml

resourcepack/
└── assets/
    └── doudizhupaper/
```

约束：

- 所有新配置项都必须提供合理默认值。
- 配置项要有中文说明。
- 资源路径、模型路径、音效键必须与代码引用保持一致。
- 修改 GUI 项或显示模式时，要同步更新：
  - `config.yml`
  - 对应 GUI 入口与 lore
  - 点击处理与实际渲染接线

## 生命周期建议

初始化顺序建议保持为：

1. 读取 `config.yml`
2. 读取 `gui-icons.yml`
3. 初始化存储
4. 初始化第三方兼容层
5. 初始化牌桌管理器 / GUI 服务 / 音效与音乐协调器
6. 注册命令、监听器、占位符
7. 恢复已有牌桌
8. 同步 CraftEngine bundle / 资源状态
9. 开始正常对局服务

重载时必须注意：

- 不要直接破坏玩家当前手牌状态
- 尽量只刷新显示层
- 必要时走现有的 `reloadVisualState(...)` 链路
- 避免全量销毁世界实体造成闪屏或交互丢失

## 开发优先级

功能开发请优先按以下顺序推进：

1. 先保证**斗地主核心回合状态机**正确。
2. 再保证**世界实体显示稳定**，不闪烁、不乱转、不残留错误状态。
3. 再统一**GUI 设置与管理员调参入口**。
4. 再补强**音效、动作、提示、剩余手牌提醒**等表现层。
5. 再完善**持久化、恢复、历史记录**。
6. 最后再扩展德州或其他牌桌玩法。

## 明确禁止事项

- 禁止把本项目当成通用菜单插件、商店插件、邮箱插件来设计。
- 禁止在每次状态变化时全量重建所有显示实体，能复用就复用。
- 禁止让预览牌、按钮、座位显示因为刷新策略产生闪烁、跳角度、回弹错乱。
- 禁止在异步线程中直接操作 Bukkit / Paper 实体与玩家界面。
- 禁止把规则判断、渲染判断、存储逻辑混在同一个超大方法中。
- 禁止硬编码与当前资源包命名空间不一致的资源键。
- 禁止忽略旧配置兼容，尤其是布尔开关升级为枚举/模式时。
- 禁止新增玩家可见文本时绕过 `MuzTheme / Component / MiniMessage` 风格体系。
- 禁止为了“看起来快”而牺牲状态一致性与渲染稳定性。

## 工作准则

后续一切实现都应以以下优先级为最高原则：

1. **回合规则正确**
2. **显示状态一致**
3. **实体刷新稳定**
4. **玩家交互清晰**
5. **资源与配置可维护**

若实现便利性与架构清晰度冲突，优先选择更清晰、可维护、可扩展的设计。
若表现效果与稳定性冲突，优先保证稳定性。
若新增功能会破坏既有牌桌手感，优先保住现有交互与视觉一致性。
